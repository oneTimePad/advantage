syntax = "proto2";

package advantage.protos;

enum Activation {
    NONE = 0;
    RELU = 1;
    SIGMOID = 2;
    ELU = 3;
}


enum Initializer {
    ones_initializer = 0;
    variance_scaling_initializer = 1;
}
